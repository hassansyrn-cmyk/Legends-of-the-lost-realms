# Project guardrails for AI agents working on Legend of the Lost Realms

Unity project lives in the inner folder `unity-3d` (Unity 6000.6.0f1). Branch: `chatgpt-astra/unity-3d`.

## CRITICAL: Aster character animation glitches

The character ("walk glitch", "growing/teleporting at walk start", horizontal snapping at idle->walk->run, sinking into the floor) has been FIXED. Do not regress it.

How the fix works (restore exactly this if it ever breaks):
- `unity-3d/Assets/Editor/AsterPhase1.cs` > `NormalizeRoot()` flattens **ALL** `RootT` curves (x/y/z) to constant 0 for every baked clip. The game capsule supplies all locomotion; ramped Mixamo root curves are the glitch source.
- `BuildPrefab()` reads `baselineRootY` from the idle source clip's `RootT.y` and pushes the model's `localPosition.y += baselineRootY * scale` so the sink from zeroing the root is baked into the prefab geometry.
- The clip files `unity-3d/Assets/Resources/Animations/Aster/*.anim` are **generated assets**. They are written ONLY by the Unity menu **`Lost Realms > Phase 1 > Prepare Aster Mixamo Character`**.
- After ANY change to an Aster clip FBX, the base model, or `AsterPhase1.cs`, regenerate via that menu. Never hand-edit or delete those `.anim` files, and never "fix" them another way.

What has gone wrong in the past (do not repeat):
- Replacing weapon FBX / reimporting weapon assets caused the fixed `.anim` clips to be lost; a rollback (`git reset --hard c1e81dd`) restored old glitchy clips and the glitch returned. The working commit for character animation is `c1e81dd`.
- A committed `.anim` set can be stale vs. the fixed baker; if the glitch reappears, first re-run the Phase 1 menu before changing any code.

## Per-weapon attack animations (Sep 2026 user pass, reversible)

- `WeaponCatalog.AttackStyle()` maps grip → style by `EquipEuler`: `(8,0,0)` axes/maces/hammers → `chop`, `(10,0,0)` spears/poles → `spear`, empty resource (fists) → `unarmed`, everything else → `slash`. `Hero.Attack` keeps `attackState` as `attack_1/2/3` (draw timing, Vfx, slash arcs all key off the `attack_` prefix) and passes the style separately to `PlayAttack`/`ClipLength`, which route to the style clip arrays with silent fallback to slash — a missing style file can never break attacks, and `charged` stays global (parry brace depends on it).
- Style sources are 5 Mixamo takes (`Aster_Take_Downward/ClubCombo/GSJump/Boxing/Kick` from `~/Downloads/Aster 3D Model/`) plus 6 Sword-and-Shield-pack takes (`Aster_Take_SnSKick/SnSAttack2/SnSAttack3/Block/Hit2/Hit3`, rig-only FBX copied raw — do NOT Blender-strip takes: Unity imports no take from armature-only re-exports and re-export breaks the centimeter scale the avatar copy needs), baked by the same `Prepare()` run into 12 new states (`chop/spear/unarmed_1/2/3` + `block`/`hit_2`/`hit_3`) with identical root-flattening. Current mapping: chop={Downward,ClubCombo,GSJump}, spear={Boxing,SnSAttack2,SnSAttack3}, unarmed={MixamoKick,Boxing,SnSKick} — several states share one take file; each spec renames + bakes independently, so sharing is safe.
- `block` plays on parry when armed (fists keep the `charged` brace) via `ParryState()`; hits randomly play `hit/hit_2/hit_3` via stored `hitState` (chosen once in `Damage()`, since per-frame `Play` would restart). `Names[]` appended (indices 12-14) so the base 12 are untouched; blend rate covers the new states.
- 6 more weapons (ids 29-34) from unused PurePoly pack models, converted by plain copy+rename (`Resources/Weapons/<Name>.fbx`, palette copies as `Textures/<Name>_basecolor.jpg`, no normal maps): RangerAxe/Executioner (chop), SquireBlade/Juggernaut/MoonChakram (slash), HuntsmanSpear (spear). Save clamp is `-1,34`, drop pool `random.Next(1,35)`; styles assign automatically by grip. Weapon FBX must stay under 2,000,000 bytes (these are 45-179 KB).
- 6 more weapons round 2 (ids 35-40): RiftDagger (slash), WardenPike (spear), Maul/EmberTorch (chop), SageStaff from the Blink pack (spear; `Staff_Basic_Basecolor.png` resized to 512 JPG) + BrassFangs knuckles (forced `unarmed` style by id in `AttackStyle`). Clamp `-1,40`, pool `random.Next(1,41)`.
- **Level portals upgraded:** `RealmWorld.Gate` adds 3 counter-rotating rune halos (spun in `RealmGate.Update`), side crystals, a translucent beam, a point light on its own `Gate light` child, a LightPillar burst, and a 1.25× boss scale. NEVER position via `light.transform` (or any component's `.transform`) when the component sits on the prop root itself — that moves the whole object (this exact bug teleported every gate to (0,2,0)); always pose a dedicated child object. Gate regressions: `Assets/Editor/GateProbe.cs` (`-executeMethod LostRealms.GateProbe.Diagnose`) builds level 1 headless and dumps the end island + gate subtree to `Validation/gate-diagnose.txt`. Round-4 undo: `Temp\opencode\restore_round4.ps1` (+`anim_round4_undo\`).

## Round 5 — gameplay / graphics / physics pass (Sep 2026, all reversible)

- **Post FX**: `Assets/Resources/Shaders/RealmPost.shader` (`LostRealms/RealmPost`: grade + vignette + cheap 4-tap bloom) driven by `Assets/Scripts/RealmPostFx.cs` on the Adventure Camera (BIRP `OnRenderImage`). Loaded via `Resources.Load<Shader>("Shaders/RealmPost")` so it survives build stripping — do NOT switch to `Shader.Find` without adding it to Always Included Shaders (that is how the sky shader went magenta once). Per-realm tint/bloom set in `RealmAtmosphere`; toggle `Save.postFx` in the Sanctuary. Validate with `Assets/Editor/PostProbe.cs` (`-executeMethod LostRealms.PostProbe.Diagnose` → `Validation/post-probe.txt`).
- **Camera feel** (`FollowCamera`): FOV kick from `Hero.Dashing/Attacking/Pulling`, `ZoomBias` (boss load zooms in, `Defeat()` pulls back), `Kick(dir)` directional jolt on damage, obeys `Save.shake`.
- **Ambience**: aurora ribbons (frozen realm), drifting embers/snow (`CreateAmbience`), cloud sea and islets unchanged.
- **Impact**: `Assets/Scripts/ImpactMarks.cs` (capped scorch decals, 14 max) placed on deaths/spell detonations; landing dust in `Hero.FixedUpdate` (`fall<-7`).
- **Projectiles**: charged spell (hold F / SPELL) lobs an arcing AoE (`SpellBolt` lob mode: gravity, 2.8 m detonation, bigger damage); Moon Chakram's charged swing throws a returning disc (`ChakramProjectile`).
- **Destructibles** (`Assets/Scripts/Destructibles.cs`): `BreakableCrate` (blade-smashable, static `All` list, shard burst + coin chance), `PushableBlock` (Rigidbody shoved via `Hero.OnControllerColliderHit` — keep that handler on the Hero class, not CharacterVisual), `LanternSwing` pendulum.
- **Enemy AI/affixes** (`Enemy`): non-boss stage>=3 enemies roll one affix — 1 Shielded, 2 Burning, 4 Swift, 8 Vampiric, 16 Armored — with a coloured `Art.Ring` tell; aggro sharing within 8 m; ranged kinds (7/13/17) kite under 3.4 m; `MoveTo` adds a per-kind flank offset; Elite (14) fires a 2-hit combo.
- **Movement kit**: one air dash per airtime (`airDashes`), variable jump (release trims rise via `g.JumpHeld`), grapple to the next route island (desktop `G` only — mobile has no 7th action button; the 6 round buttons fill the bottom-right cluster within 1280). `Hero.Pulling` drives the FOV kick.
- **Weapons**: `WeaponCatalog.Affinity` element per weapon (EmberTorch=ember; Hovl*/SageStaff=frost; MoonChakram/PureScythe/HalberdC=gale) gives ×1.15 vs matching weakness in `Hero.Attack`; Sanctuary `ARSENAL RANK` (0-3) adds +8%/rank to all weapon damage (`damage*=1f+Save.arsenalRank*.08f` — the guarded `float damage=...*weapon.Damage;` literal must stay intact for `verify-weapon-system.sh`).
- **UI**: route compass in the HUD (points at the next island, then the gate); boss bar already existed. HUD remains IMGUI (touch rects in `TouchRouter.ActionRect` must stay in sync with `RealmGame.RoundButton`).
- **Touch UI pass (Sep 2026):** the mobile HUD is a circular action cluster, not a row — `TouchRouter.ActionRect` is the single source of truth for both hit-testing and drawing: BLADE primary bottom-right `Rect(1082,530,160,160)`, JUMP `(964,605,100,100)`, DODGE `(856,608,88,88)`, POWER `(758,614,80,80)` (bottom movement row), PARRY `(964,495,84,84)` and SPELL `(1059,425,88,88)` arced above the attack button. `RealmGame.RoundButton` draws the circles with the procedural `circleFill`/`circleRing` textures built in `Styles()`; the joystick is a fixed base ring at (150,612) with a knob tracking `touch.Move`. The main menu was redesigned to a centered composition (eyebrow, `titleC`, divider, journey status line, stacked 380-460px buttons). The "Particle Velocity curves must all be in the same mode" device spam was `RealmAtmosphere.CreateAmbience` leaving `velocityOverLifetime.z` in constant mode while x/y were two-constant — all three axes must share one mode; rain volumes now go through the public `Vfx.Repair`.
- Round-5 undo: `Temp\opencode\restore_round5.ps1` (+`round5_undo\` snapshot of all scripts) removes the new scripts/shaders/textures and restores the gameplay scripts.
- One-click undo: `Temp\opencode\restore_anim_undo.ps1` restores the 12 original `.anim` + 5 scripts from `Temp\opencode\anim_undo\` and deletes the 11 take files, 12 new clips, and 6 weapons + textures. No re-bake needed.
- **Spell skill (Sep 2026 user pass):** F key / SPELL touch button (6th action; see the Touch UI pass above for the current cluster rects — keep all four files in sync: `TouchRouter` role+rect, `RealmGame` flag+input+HUD, `Hero` handler). `Hero.CastSpell()` (0.8s cooldown, 15 energy, fails like `Cast()` when broke) plays the baked `cast` state (SnS casting take) + muzzle flash and fires a `Hero.SpellBolt`: speed 14, 2.2s life, WEAK homing only (20° cone, 8m, steer 2.0 — aim matters), hit radius +.38, spiral wobble + pulse on the visuals. Damage 2.2+powerRank×.45 as elemental `Power`. The spell upgrades through the Sanctuary Elemental Power track: bolt/trail/impact scale + heat toward white per rank (rank in `SpellBolt`). Bolt look is code-built from in-project assets (tinted shard + Kenney flare/smoke + attached `eric_FX_Fireball` / mayker water-electric projectile shells). Bonus asset wiring from the same pass: `ga_vfx_Shield_01` on parry success, `eric_FX_LightPillar` on weapon-drop spawn, dash `ga_vfx_Hyperdrive_01`, boss-death `ga_vfx_MeteorRain_01`, summon `ga_vfx_Implosion_01`, weak-hit `eric_FX_Purple_Hit_02`, frozen-realm ambient `ga_vfx_Rain_01` volumes (raw instantiates, no Kill). Spell-only undo: `Temp\opencode\restore_spell.ps1` (+`anim_spell_undo\` snapshot; keeps styles/weapons).

## Locked physics (do not change)

`unity-3d/Assets/Scripts/Hero.cs`:
- All movement in `FixedUpdate` with `Time.fixedDeltaTime` (set to 1/60 in `RealmGame.Awake`); single `Controller.Move` per step.
- `MaxMoveSpeed=4.8f`, `GroundResponse=18f`, `AirResponse=8f`, `StopResponse=22f`, dash 9.5/0.22s/0.9s, jump vertical 8.4, apex float 11/23, fall clamp -20, jumpGrace 0.14, jumpBuffer 0.13, knockback 4.5.

## CI guards

- `.github/scripts/verify-aster-motion-tuning.sh`: `Hero.cs` must contain `const float MaxMoveSpeed=4.8f`, `Vector3.MoveTowards(velocity,desiredVelocity,response*dt)`, `"double_jump"`; forbidden `Visual.Restart("double_jump")`, `wish*6.1f`, `speedRatio*.82f`, `SetSpeed(3.1f)`.
- `.github/scripts/verify-weapon-system.sh`: `WeaponSystem.cs` must contain `Weapons/Aster_Axe`, `public sealed class WeaponDrop`, `public sealed class EquippedWeapon`, `model=created?created.transform:null;`; weapon FBX must stay under 2,000,000 bytes.

## Combat feel (added Sep 2026, do not regress)

`unity-3d/Assets/Scripts/Enemy.cs`:
- Per-kind tuning tables `NoticeRange`, `ChaseSpeed`, `MeleeDamage` (18 entries now) used in `Update()` / `CommitAttack()`. Kinds 0-10 are the original families/bosses; 11 Flyer, 12 Bomber (exploder), 13 Summoner, 14 Elite, 15 Skeleton, 16 BriarGoblin (rigged Forest_Goblin, sickle melee), 17 EmberDemon (rigged winged demon, fire bolts). `RealmWorld.Build` spawns the new kinds from stage>=3 (skeleton kind 15 uses `stage>=4&&i%7==6` — i=6; it must stay on a free index, the old `i%4==1` was shadowed by the kind-14 `i%6==5` and kind-12 `i%5==4` rules and never spawned; kind 16 uses the only other free slot `stage>=5&&i==7`; kind 17 displaces Flyer via `stage>=9&&i%6==2` placed FIRST in the chain, and Summoner minions are kind 16 from level>=7).
- New behavior: Flyer (`aerial`) hovers 2.3m up, bobs, ignores the ground clamp, and has no blocking collider; Bomber detonates a `StrikeZone` and self-destructs; Summoner holds range and spawns capped (16) goblin minions (kind 16 from level 7+); Elite has 2× health, a bigger model and `MeleeDamage` 3.
- Every enemy gets `EnemyAura`: 3 orbiting motes tinted to its weakness element via `RealmGame.ElementColors`.
- Hit feedback: `DamageTip` floating damage numbers + `HitSpark` bursts on all hits (enemy melee, bolts, telegraphed strikes, and hero attacks). The enemy emission flash on damage was REMOVED per the user (no orange tint on damage/death); the red charge glow during `State.Windup` is kept as the telegraph.
- Enemies block the player: `Enemy.Configure` adds a non-trigger `CapsuleCollider` (no contact damage — damage stays manual), and `MoveTo()` keeps enemies from crowding inside the hero.
- Death (`EnemyDeathFall`): the dying animation plays (rigged enemies run their death clip, static meshes tip over), then the body bursts to ash and vanishes **in place** — it never moves down through the platform. Do not reintroduce the falling version the user rejected.
- Do not remove `DamageTip`, the windup `Glow`/`ClearGlow`, the stat tables, the aura, or the enemy collider without user approval.

## Combat response (added Sep 2026, do not regress)

Player defense + elemental weakness layer:
- **Perfect dodge**: `Hero.Damage()` rewards a hit shrugged off inside the active dash window (`Elapsed<dashUntil`): +30 energy, a counter opening, and a short `HitStop`. `dodgeRewarded` gates it to once per dodge (reset when the dash starts).
- **Parry**: `Hero.Parry()` opens a `.2s` negation window (`.55s` cooldown, grounded only). `Hero.ParrySuccess()` negates the hit, stuns the nearest attacker (`Enemy.Stun`), refunds 20 energy, opens a counter, and fires `HitStop`. Aster has no parry clip, so it braces in the `"charged"` pose. Input: desktop `L`, mobile `PARRY` button.
- **Counter**: after a perfect dodge/parry, `counterUntil` makes the next `Hero.Attack` deal ×1.6 with a golden spark; HUD shows `COUNTER READY`. Do not change `Hero.CounterReady`.
- **Elemental weaknesses**: `Enemy.Weakness` (18 entries, incl. Skeleton/BriarGoblin/EmberDemon) + `public int WeakElement`; matching the player's element (`Power` 0 ember / 1 frost / 2 gale) boosts damage ×1.5 and forces the elemental reaction even on physical melee (`Hero.Attack` passes `Power`). The enemy health bar fill + pip use `RealmGame.ElementColors[foe.WeakElement]`.
- `RealmGame.HitStop(seconds,scale)` dips `Time.timeScale` briefly; `RealmGame.Update` restores it via `Time.unscaledTime`. Physics stays on the constant fixed step, so this only slows pacing, never the step. Do not remove the restore line before `if(!I||!Player)return;`.
- The action cluster rects (`TouchRouter.ActionRect`) and the drawn circles (`RealmGame.RoundButton`) are deliberate; keep both in sync (the rects are the single source of truth — see the Touch UI pass).

## Weapons

- `unity-3d/Assets/Resources/Weapons/*.fbx` are generated from the user's decimated GLBs (`AXE_decimated.glb`, `Long_Sword_decimated.glb`, `Curved_Sword_decimated.glb` in `~/Downloads`) via Blender 5.2 (`C:\Program Files\Blender Foundation\Blender 5.2\blender.exe`). Keep each FBX under the 2,000,000-byte CI budget.
- Textures live in `Resources/Weapons/Textures/<name>_basecolor|_normal|_rm.jpg`. `WeaponCatalog.WeaponSkin()` builds the runtime Standard material from `_basecolor` + `_normal`; the `_rm` (glTF packed) map is reference only and is not bound to Standard.
- Normal maps need `textureType: 1` (NormalMap) in their `.meta`; the basecolor maps import at default (sRGB).
- Weapon positioning/orientation is driven by `WeaponDefinition.EquipEuler` and `ModelScale` in `WeaponSystem.cs`.
- **Asset Store weapons:** Blink `BlinkAxe=4`/`BlinkMace=5`/`BlinkSpear=6`, MYFG `AxeIron=7`…`SwordGreat=16`, Hovl `HovlMagic=17`/`HovlMoon=18`/`HovlMoon2=19`, PurePoly `PureAxe2H=20`…`PureSword1H=25`, and `HalberdA/B/C=26..28` use imported low-poly models `Resources/Weapons/*.fbx` + `Resources/Weapons/Textures/*_basecolor.jpg` (Hovl from their albedos, PurePoly all share the converted `PP_Color_Palette`, halberds from their `color.tif`s). `RealmWorld.Build` forces chapter 1's drop to the greataxe (`stage==1?4:random.Next(1,29)`); the save clamp is `0,28`. 29 definitions total; all weapon FBX stay well under the 2 MB budget.
- `EquippedWeapon` keeps the weapon **sheathed on Aster's back** and draws it into the right hand during attacks (state starts with `attack_`/`charged`), re-sheathing ~1.1s after. The sheath is **geometry-based**: `Measure()` reads the weapon's real bounds (pivot-agnostic), anchors to the chest bone with `BackGap`, stands the blade up the spine, and twists so the flat lies against the body. The hand pose is driven by each `EquipEuler`. Do not reintroduce fixed local-space `SheathOffset`/`SheathEuler` — they float/clip because the FBX pivots are arbitrary.

## Enemy models (new families)

- The 4 new families use the user's high-detail showcase meshes from `~/Downloads/converted_models` (14 FBX, ~480k tris, 2048² basecolor, no rig), decimated+grounded+texture-reduced via Blender 5.2 and exported to `unity-3d/Assets/Resources/Characters/<Role>.fbx` (~17-26k tris, ~1.1-1.3 MB each):
  Flyer=`model_11` (energy orb), Bomber=`model_12` (lava rock golem), Summoner=`model_02` (wizard), Elite=`model_09` (crystal knight).
- Basecolor maps live at `Resources/Characters/Textures/<Role>_basecolor.jpg` (512²), bound at runtime by `CharacterVisual.CharacterSkin()` (same pattern as `WeaponSkin`). Roles without a texture are untouched, so the original prefab families keep their materials.
- These meshes have **no skeleton**, so `Enemy.Configure` adds `EnemyIdleMotion` (procedural bob + sway) when the visual has no `Animator`. If a model faces the wrong way, set the yaw in `EnemyIdleMotion.Setup` in `Enemy.Configure` (currently `0f`; `180f` if backward).
- The remaining 10 source models are unused and could restyle the existing families/bosses later.
- **Skeleton (kind 15, added Oct 2026):** imported Dzen skeleton (`Assets/Skeleton/Model/Skeleton.FBX`, ~2.5k verts, static mesh, no UVs) converted to `Resources/Characters/Skeleton.fbx` (~200 KB, 1.82 m tall, feet at origin) + `Resources/Characters/Textures/Skeleton_basecolor.jpg` (512²). The source pack's FBX is **ASCII/legacy** — Blender 5.2 can't read it, so the pipeline is: `fbx2obj.ps1` (PowerShell ASCII-FBX→OBJ parser) → Blender `wm.obj_import` (the OBJ/PLY/STL importers moved to `bpy.ops.wm.*` in Blender 5; `io_scene_obj` was removed) → ground to minY=0 → uniform-scale to 1.82 m → `uv.smart_project()` (mesh has no UVs) → binary FBX export. `Enemy.Configure` adds `EnemyIdleMotion` because the mesh has no `Animator` (`kind>=11`).
- **Skeleton import quirk (fixed):** a naive Blender FBX export of the skeleton imports into Unity with a `(270,0,0)` root rotation and the model lying/flattened (Blender reads the same file as upright). The fix is baked into the mesh: after the `+90°` X pre-rotation the height axis becomes Z, so the export must also shift the vertices so `minZ=0` (not `minY`). The corrected file imports as `Skeleton.fbx | root rot (270,0,0) | size 2.687,1.82,1.13 | pivotMinY 0.02` — standing, feet at origin, rendered correct despite the root rotation (only `localPosition` is zeroed at runtime, rotation is kept). Do not re-verify orientation with Blender probes alone — always check `Validation/prop-census.txt`.
- **Skeleton head-down flip (fixed Sep 2026):** the census "standing" check only verifies extents — it cannot tell head from feet — and the file actually had the skull at file z-min (single crown tip at z=0.348, feet pair at z-max), so Unity mapped the skull to world bottom and the enemy stood on its head. Fixed in Blender (`Temp\opencode\skeleton_fix5.py`, backup at `Temp\opencode\Skeleton_backup_headdown.fbx`): 180° rotation about Y through bbox-center X (proper rotation, det=+1, so winding/normals/UVs survive; facing preserved, L/R mirrored), re-shift to identical z bounds `0.348..2.168`, plus arms reposed ~60° down about shoulder pivots (|x|>0.28 verts, pivot ±0.25) to kill the T-pose. Same `export_scene.fbx` settings as before (`wm.fbx_export` does not exist in Blender 5.2 — always keep the fallback). Result: skull-crown tip at file z-max, feet pair at z-min, height/feet unchanged (`size 1.855,1.82,1.13 | pivotMinY 0.02`; census line updated by hand-mapping file bounds through world=(x-0.012, z-0.328, -y-0.595)). Lesson: after any skeleton re-export, verify head-vs-feet geometrically (extremal verts along file Z: single crown tip must be at z-max, feet pair at z-min) — census extents alone will pass an upside-down model.
- **Rigged families 16/17 (BriarGoblin/EmberDemon):** `~/Downloads/Enemy_Rigs_Package/*_Rig.fbx` (34/42-bone FK rigs, 58k tris, 2048² embedded textures) converted via `Temp\opencode\rig_prep.py` (Blender 5.2): decimate ~24k tris (groups survive collapse), scale armature to 1.30/1.80 m + ground, basecolor → `Resources/Characters/Textures/<Role>_basecolor.jpg` (512², auto-bound by `CharacterSkin`), export rigged FBX (~1.1 MB, `Validation/prop-census.txt` lines). Driven by `SkeletonLimbMotion` with per-role bone names (`upper_arm.*`, demon `wing_upper/lower.*` flap) — no Animator, so death tip-over is untouched. Forest_Elemental rig is staged but unused; Mutant Mixamo clips don't fit these custom rigs (procedural driver instead).
- **Skeleton rig + weapons + limb motion (Sep 2026):** the mesh is 68 disconnected shells including two loose weapon shells (48-vert back piece, 38-vert front-left blade) that rendered as floating debris. `Temp\opencode\skeleton_rig.py` builds a 15-bone armature (`hips/spine/chest/neck/head`, `upperarm/forearm/hand`, `thigh/shin` × L/R; no feet — foot shells ride `shin`, backup at `Temp\opencode\Skeleton_backup_unrigged.fbx`), seats the blade in the −x palm pointing down (hilt found by cross-section profile: wide end = guard) and straps the shield plate onto the +x forearm, then rigid-skins every island 100% to its nearest bone (Blender auto-weights fail on 68 disjoint shells — heat leaves 2401/2487 verts unbound — so never trust AUTO here; verify `VERTS_ZERO_WEIGHT 0`). Same FBX settings + `add_leaf_bones=False` (Blender re-import synthesizes `_end` nubs; Unity doesn't). `Enemy.cs` `SkeletonLimbMotion` (kind 15 only) poses limbs procedurally in world space about the enemy's lateral axis from rest locals (walk swing, knee bend, counter-swing arms, overhead strike on `attack_1`) — no Animator, so `EnemyDeathFall` tip-over and `EnemyIdleMotion` bob are untouched. Unity-side ground truth is `size 1.872,1.824,0.704 | pivotMinY 0.016`, root rot now `(0,0,0)` (the axis conversion moved onto child nodes when the armature was added — the file→world map is now `(x,y,z)->(-x,z,-y)`, still file-z-up). Orientation is proven HEAD_UP by `Assets/Editor/SkullCheck.cs` (`-executeMethod LostRealms.SkullCheck.Run` → `Validation/skull-check.txt`: head bone y 1.8 > hips 1.0, crown cluster on top) — always re-run it after touching the skeleton; census extents can never prove head direction. (The old 1.13 depth span was the two floaters.)

## Runtime VFX layer (added Oct 2026, do not regress)

- **`Vfx.cs`** (`Assets/Scripts/Vfx.cs`) is the one-shot particle spawner for the imported effect packs. API: `Vfx.Play(key,pos,rot=default,scale=1,parent=null)` spawns a VFX prefab rooted under the realm world and `Vfx.Attach(key,parent,localPos,localScale)` mothers a looping trail (weapon glow) to the hero; both add `VfxKill` so effects auto-destroy after their longest particle lifetime (loopers get ~2.5 s). `Vfx.Slash(power)` maps element 0/1/2 to the Mayker fire/water/electric slash prefabs.
- Prefabs live in `Assets/Resources/VFX/` with the `ga_` (Gabriel Aguiar BIRP), `eric_` (Eric VFX), and `mayker_*` (Mayker slash) prefixes, loaded via `Resources.Load<GameObject>("VFX/<key>")`. **Missing prefabs are a silent no-op by design** — nothing crashes if an effect is renamed or removed.
- Kenney particle sprites (`Assets/Resources/VFX/Textures/smoke_*.png`, `flare_01`, `spark_02`, `light_02`, `magic_03` from the Kenney particle pack) feed code-built effects: `Vfx.KenneyPuff.Burst` (billboard puff bursts, used on projectile impacts) and `EnemyBolt`'s flare core + smoke trail. `EnemyBolt.Create(p,dir,speed,damage,boss,kind)` renders a spinning glow shard (kind 17 = red fire shard ×1.2, boss = violet ×1.7, else ember) instead of the old primitive sphere; callers pass 4 args by default.
- The project **requires the Particle System built-in package**: `com.unity.modules.particlesystem` is in `Packages/manifest.json`. Do not remove it — `Vfx.cs`, the VFX prefabs, and `Assets/Projectile.cs` all use `ParticleSystem`, and without the module Unity fails compilation with `CS1069` (your play-blocker for the imported VFX).
- Hook sites (all one-liners, keep them): `Hero.Attack` slash VFX; `Enemy.Hit` impact/explosion (elemental hits use `ga_vfx_Explosion_01`); boss phase `ga_vfx_Shockwave_01`; boss death `ga_vfx_Explosion_01`; `EnemyBolt` impacts `ga_vfx_Impact_01`; `RealmWorld` coin `ga_vfx_Sparks_01`, gem `eric_FX_LootDrop_Blue`, heal `ga_vfx_Heal_01`, checkpoint `ga_vfx_Portal_01`, gate `ga_vfx_Portal_02`, weapon drop `ga_vfx_LootDrop_01`, boss spawn `eric_FX_Magic Door_Gold`; `RealmGame.Respawn` `ga_vfx_Portal_01` and `RealmGame.EquipWeapon` `Vfx.Attach("eric_FX_Weapon Effect",…)`.
- Imports came from `AssetStore` packages (Gabriel Aguiar FreeQuickEffects BIRP, Eric VFX Collection, Mayker Slash VFX, Dzen Skeleton, Polygon Blacksmith skeletons, Ink Phantom Desert City, HobiSoLoved Snow) extracted under `C:\Users\X1 YOGA\AppData\Local\Temp\opencode\vfx\`; **PowerShell functions do not persist across tool calls** — reuse the inline import function inside the same call.

## Environment props

- Imported low-poly nature packs (SimpleNaturePack/B&J Studio, k0rveen, BrokenVector, CreativeCup, Gigel3d) live under `Assets/`. Their scripts/demo scenes/archives were removed so the minimal project keeps compiling — keep it that way.
- `RealmProps.cs` scatters selected models around each island at build time (`RealmWorld.Build` calls `RealmProps.Scatter`). Models are copied to `Resources/Props/Nature/*` (SimpleNaturePack), `Resources/Props/Desert/*` (Polylised desert city: House/Ruin/Tower/Gate/Tent/Church/Wall) and `Resources/Props/Snow/*` (HobiSoLoved: Pine/Tree/DeadTree/Cottage/Fence/Sign/Snowman/Well/Stump/Bridge) with fresh GUIDs. One Standard material per realm is built from `Resources/Props/Textures/Nature_basecolor.png` (realm 0 lush), `Desert_palette.png` (realm 1 sandy, generated 64² via System.Drawing), or `Nature_snow.png` (realm 2). The `Folder(realm,name)` helper routes a prop name to the right `Resources/Props/<folder>`. Do not delete those textures.
- **Desert/snow props import with a baked root rotation** (e.g. `Church_01.FBX | root rot (90,180,0)`, `House_01.FBX | (270,270,0)`, snow `Pine_01 | (270,180,0)`). The import rotation IS what stands them up in Unity — `RealmProps.Place` must preserve it (`var baseRot=source.transform.localRotation; … prop.transform.localRotation=Quaternion.Euler(0,yaw,0)*baseRot;`). Never reset those to `Quaternion.identity` or the props lie flat. `AddCollider` converts the world AABB into the prop's local frame via `InverseTransformPoint` on all 8 corners (never the raw `bounds.size`, which reads the height on the wrong local axis for rotated imports); tree capsules pick the direction with the longest local extent.
- `Validation/prop-census.txt` is generated by the editor menu `Assets/Editor/PropCensus.cs` (`LostRealms.PropCensus.Run`, batch: `-executeMethod LostRealms.PropCensus.Run`) and prints per-asset `root rot | size | pivotMinY` for every prop/character fbX — the ground truth for import orientation. Re-run after touching any imported model.
- **Desert stage landscaping (Sep 2026 user pass):** `RealmProps.PlaceDesertBuildings` now puts desert buildings in **organized, path-safe rows** — flanking the travel lane at fixed offsets (`x=±min(max(width*.5-1.05,1),4.2)`, `z≈±length*.22`, ≤2-3 per island) with the same facade toward the path, instead of random edge/mid-lane scatter that could sit on the route. `Gate_01` is optionally built **across the lane** as a pass-through arch (`localRotation=AngleAxis(90,up)*baseRot` puts its portal down the travel axis). Buildings get **real-mesh collision** (`MeshCollider`, concave, per renderer mesh child) so doorways/gates stay hollow while walls block — never re-add full `BoxCollider`-only covers for `Gate/Church/House/Ruin/Tent/Tower/Wall/Cottage/Bridge`. The ground-truth of which opening each desert model actually has is `Validation/door-facing.txt` (only Gate_01 has a reliable ≥1.3 m portal at 3 m height; the rest are flush facades). Facing derives from **side sign only** (`side==-1→0°, side==1→180°`), never from door-axis analysis: the pack's import rotations swap axes unpredictably, so author/root-space derivations break (see DoorScan.cs notes).
- `Assets/Editor/DoorScan.cs` (`LostRealms.DoorScan.VerifyFacing`) measures ground-level openings by instantiating each desert prefab with the runtime pose `AngleAxis(yaw,up)*baseRot` at 3 m height and sampling ground/roof occupancy; it exists to sanity-check facing and collide with reality, not to be relied on for per-face door axes.
- **Sky (floating-islands look):** `Assets/Shaders/RealmSky.shader` (internal name `LostRealms/RealmSkyBox` — must stay UNIQUE; a pre-existing `Assets/Resources/Shaders/RealmSky.shader` already owns `LostRealms/RealmSky` with a `_Zenith`/`_Horizon` interface, and duplicate shader names make `Shader.Find` resolve nondeterministically, which once produced a magenta sky) with per-realm materials built in `RealmAtmosphere` (horizon color == fog color, so the seam vanishes); the shader is pinned in `ProjectSettings/GraphicsSettings.asset` → Always Included Shaders or APK builds strip it. ShaderLab/CG gotchas that bit once: HLSL uses `frac`, not GLSL `fract` (import parsing + `isSupported` will NOT catch it — only a real GPU variant compile fails, i.e. magenta in game but clean batch logs); verify with `Assets/Editor/SkyProbe.cs` (`-executeMethod LostRealms.SkyProbe.Diagnose` in a foreground run renders both skyboxes off-screen → `Validation/sky-diagnose.txt` with magenta%). `RealmAtmosphere` also runs a one-draw-call particle cloud sea (Kenney smoke, tinted per realm) far below the route and 5 bobbing rock islets. Camera stays `clearFlags=Skybox`.
- **Extra props (`RealmProps.PlaceOrnaments`):** petrified guardians (fallen Flyer/Bomber/Summoner/Elite meshes under a shared stone material, `Prop Statue *` → plain box collider) flanking desert gate arches and boss arenas; realm-tinted glow crystals (octahedron `ShardMesh` + cached emission materials); desert banners; realm-2 lanterns (glow orb + ring). All call the shared `AddCollider`, so nothing new is walk-through.
- `RealmScenery.IslandTop()` replaces the square slab top with a rounded, irregular ring mesh (the `Realm surface` cube renderer is hidden but kept as the size reference; `Walkable stone` still supplies collision). Ring quads must wind up (fan + strips face +Y) or the whole top inverts and the middle turns transparent. Distant background crags also get tops.
- Scattered props block the player: `RealmProps.AddCollider` gives trees a `CapsuleCollider` (~0.3 m) and rocks/bushes a shrunk `BoxCollider`; scenery oaks/pines get trunk capsules and edge rocks get sphere colliders. `Art.Shape` destroys colliders unless `solid=true`, so pass-through returns whenever new shapes are added without explicit colliders.
- **Unarmed start:** `Save.equippedWeapon` `-1` = fists. `WeaponCatalog.Get(-1)` returns the synthetic `FISTS` definition (empty `Resource` → no model, damage .85 / reach base .92 / tempo 1.12); `Hero.Start` skips `EquippedWeapon.Equip` whenever `equippedWeapon<0`. Fresh installs and **NEW JOURNEY** (`Screen==Menu` → resets `Save` to a fresh `Progress` and loads chapter 1) begin unarmed; a weapon drop's `EquipWeapon(id)` arms Aster mid-run. Progress' default `equippedWeapon` is `-1` and the save clamp is `-1..28`. Keep the `RuntimeProbe` equip-axe check working (it relies on `WeaponCatalog.Get` being non-null for equipped ids).
- Healing pickups are red rotating hearts (`RealmHeal`, +3 HP, spark pink).

## Audio (Modern RPG Overhaul - Sep 2026)

- **Music per stage & boss battles**: Exploration stages play their respective realm theme: `verdant_theme` (Realm 0 Verdant, levels 1-3), `desert_exploration_theme` (Realm 1 Burning Dunes, levels 5-6), `frozen_exploration_theme` (Realm 2 Frozen Peaks, levels 8-9), and `emberfall_exploration_theme` (Realm 3 Emberfall, levels 11-14). Menus play `verdant_theme` and Sanctuary plays `frozen_exploration_theme`. When fighting guardians / boss stages (levels 4, 7, 10, 15), `boss_battle_theme` plays.
- **Modern Sound Effects Suite**: All 30 `sfx_*.wav` sound effects were overhauled from retro/8-bit chiptune sounds into studio-grade modern fantasy action RPG audio (crisp blade whooshes with singing steel overtones, punchy tactile impacts with sub-bass punch, natural athletic leaps and footsteps, delicate crystalline coin and gem chimes, temple singing bowls for checkpoints, orchestral victory stinger, etc.). Regenerated via `tools/generate_modern_audio.py`. `sfx_heal.wav` provides warm divine restoration audio for heart pickups.

## Workflow

- User play-tests in the Unity Editor and cannot share screenshots; verify by asking the user.
- Only PowerShell is available (Windows; no bash/Python toolchain).
- Do not commit/push unless the user explicitly asks.
- Never run the Phase 1 menu for weapon-only changes.

## Round 9 — enemy/pickup visual repair (Sep 2026, verified headless)

- **"Flying" root cause (world vs local space):** `Renderer.bounds` is WORLD space. `Art.PickupModel` (`RealmWorld.cs`) used the 2-arg `Instantiate(prefab,parent)` (keeps prefab world pose ≈ origin) and then subtracted world bounds from `localPosition` — every coin/gem/heart mesh parked near the origin with a giant local offset and whipped around the spinning pickup root. Same bug in `CharacterVisual.Create` normalization (`Hero.cs`): world bounds folded into `localPosition` teleported enemy bodies toward the origin, swinging wide on turns. Fix: instantiate with `worldPositionStays=false`, zero local pose, measure → scale → RE-measure → shift in world space. Guarded by RuntimeProbe anchoring checks (every pickup/heart/enemy renderer within 2.5–3 m of its root, all 15 levels).
- **Gems huge:** `GemVisual.Update` stomped `Core.localScale` to ~1 every frame, erasing `PickupModel` normalization. Now preserves base scale, pulse multiplies on top.
- **Restyle rollback (user request):** the Round-7 showcase Bruce were broken beyond code repair — Goblin/Demon/Frost/Elemental/Caster takes baked to STATIC single-pose clips (frozen T-pose), Heartwood/Sunscar/Whiteout FBX were byte-identical golem triplicates (all bosses same), Flyer/Bomber/Summoner/Elite geometry collapsed to 2 cm point-clouds (28k verts in a 2 cm box — invisible). Rold `Goblin/Demon/Frost/Elemental/Caster/Heartwood/Sunscar/Whiteout.prefab` restored from HEAD (Shared-clip animation, original materials — restyle basecolor JPGs + static role clips shelved to `_backups/restyle-families/`, golem triplicates to `_backups/golem-bosses/`). Flyer/Bomber/Summoner/Elite have no committable originals (15 MB sources too heavy, no Blender to decimate) → rebuilt as primitive prefabs via `Assets/Editor/PrimitiveEnemies.cs` (`Lost Realms/Phase 2/Rebuild Primitive Enemies`; materials in `Resources/Characters/Materials/`). Diagnose via `Assets/Editor/AllRolesProbe.cs` (`Lost Realms/Diag/All Roles + Pickups` → `Validation/all-roles.txt`; `Deep Dive`/`Source Check` variants included). Do NOT re-delete these prefabs for a restyle without first proving takes animate + geometry bounds sane via the probe.
- Verified: `compile2.ps1` exit 0, Play-mode RuntimeProbe **3313 assertions PASSED**, Android APK rebuilt (`Builds/Android/LostRealms3D.apk`, ~96 MB).

## Round 8 — Realm 4 "EMBERFALL" (Sep 2026, verified headless)

- **Scope**: campaign extends 10 → **15 levels across 4 realms** (realm split `Level<=4?0:Level<=7?1:Level<=10?2:3`); titles 11-15 = Ember Foothills / Brimstone Rampart / Cindervein Gorge / Obsidian Ascent / Emberfall Summit; accent crimson `new Color(1f,.35f,.28f)`; menu "/15 CHAPTERS", treasury "/45 STARS".
- **Routes/levels** (`RealmWorld.cs`): `RouteX`/`RouteY` gained stages 11-15 (stage 15 = 8 islands with a big climb); `IsBoss` = 4/7/10/15; boss spawn `world==3?21:world+8`. Realm-3 decor: ember crystals + `EmberHotStone` tint; no villages, ember-red palettes.
- **Boss**: level 15 is guarded by **EMBERFALL WARDEN** (kind 21, DogKnight role; Weakness=2/gale, Damage=3); phase-3 attack = "EMBERFALL FLOOD"; realm-3 heralds = Spider (kind 18). Boss DisplayName array is indexed by `RealmGame.I.Realm` (`{"HEARTWOOD COLOSSUS","SUNSCAR TITAN","WHITEOUT GUARDIAN","EMBERFALL WARDEN"}`).
- **Atmosphere** (`RealmAtmosphere.cs`, all 4-element arrays): top/stone/fog/sun/sky/post tints for realm-3 (dusk crimson), rising ember motes (rate 8, vel.y .8), 28 motes, realm-3 landmark stone tint.
- **Scenery/props**: 4th textures `Art/ember_rock_tile.jpg` + palettes `Props/Textures/Ember_palette.png`; realm-3 zenith/horizon, charred snag edge decor, dusk tufts; `usedSets`/village mats clamped 0..3; `RealmProps.Set` realm-3 branch = dead trees + rocks + clutter; ember crystals `(1,.38f,.22f)`.
- **Save migration**: v2 saves with the OLD 10-length `stars`/`best` arrays are copy-migrated to 15 on load; `unlocked` clamps 1..15, equippedWeapon clamps -1..40.
- **RuntimeProbe** now covers all fifteen routes/players/guardian models, levels 11-15 spawns, warden kind-21 + WeakElement tests, realm-3 spider heralds, and captures `Validation/Emberfall.png`. Probe tips (fragile, keep): jump touch samples `ActionRect(3)=(964,605,100,100)` center (1014,655) — the old (1160,610) is now the blade rect; boss phase tests must use PHYSICAL hits `Hit(MaxHealth*.35f,-1,false)` (a weak-elemental `Hit(0,true)` gets 1.5× and skips straight to phase 3); second-wind tests need ambient enemies cleared + full health + a 2.4s wait between lethal hits (warp gives 1.2s and wind gives 2.2s immunity that silently swallow hits).
- **Headless captures crash under `-nographics`** (null renderer dies in `ParticleSystemRenderer_RenderMultipleBillboards`); run Play-test and render probes with `-batchmode -force-d3d11` instead. Editor-only probes that do not call `Quit()` need `-quit` or they hang/open an editor loop.
- **Idle-motion speeds calmed (Sep 2026, user "hover/spin fast" fix):** coins rotate 45°/s (was 110), gems 12°/s (was 20), pickup/heal bob `Sin(Elapsed*1.4)*.09` (was 2.6-2.8 × .13-.15); gem core 60°/s (was 128), shard orbit 0.8+0.07i rad/s (was 1.35+0.11i), halo 30°/s base (was 52); `EnemyIdleMotion` bob `.02`@1.1 rad/s (was .035@1.6) with ±1° pitch / ±3° sway (was 1.5°/6°); `EnemyAura` orbit .5/1/.8 rad/s per element (was .65/1.7/1.1) with slower triple-spin; heal 28°/s (was 55); weapon drop bob `.09`@1.4 rad/s + 24°/s spin (was .12@2.4 + 42°); Flyer aerial bob 1.2 rad/s (was 2.4). Magnet collection and chase speeds untouched.
- Verified: `compile2.ps1` exit 0, Play-mode RuntimeProbe **214 assertions PASSED** (`REALM_RUNTIME_TESTS_PASSED 214`), PropCensus/GateProbe/EnemyVisualProbe/DoorScan exit 0, CI verify-*.sh 5/5 (boot-smoke skipped: no Linux build on Windows), Android APK rebuilt (`Builds/Android/LostRealms3D.apk`, 96 MB).

## Round 7 — feel, restyle & dressing pass (Sep 2026, verified headless)

- **Dodge roll + hit variants**: `Aster_Take_DiveRoll/GetHitF/GetHitB.fbx` (from `ExplosiveLLC/RPG Character Mecanim Animation Pack FREE/Animations/Unarmed`) bake into `roll`/`hit_4`/`hit_5` states (baker `ClipSpec.AutoAvatar=true`). **Humanoid retarget lesson:** for these `B_*`-bone rigs, `CopyFromOther` with the Mixamo Aster avatar matches ZERO bones and Human import silently yields no clips — `CreateFromThisModel` auto-maps 49 bones and works; a hand-built `.ht` template map (29 bones) makes it WORSE. If a take ever bakes 0 clips after failed attempts, delete the `.fbx`+`.meta` and re-copy fresh (poisoned import settings persist in the .meta). Runtime: ground dash plays `roll` (`dodgeVisualState` field, 3.35× speed ≈ .4s), air dash keeps `dodge`; `HitVariant()` pool is now hit/hit_2/hit_3/hit_4/hit_5; `Names[]` indices 16-18; blend-rate + `PlayClip` speed lines cover the new states. Death already existed — untouched.
- **Enemy restyle** (`~/Downloads/converted_models` → `Resources/Characters/`, pipeline = `Temp/opencode/export_enemy_restyle.py`, preview renders in `Temp/opencode/enemy_previews/`): Goblin=model_01 (hooded rogue), Demon=model_05 (flame warrior), Frost=model_04 (ice crystal warrior), Elemental=model_06 (stone knight), Caster=model_14 (wraith), Heartwood boss=model_07 (moss golem), Sunscar boss=model_10 (sand giant), Whiteout boss=model_08 (ice golem); spares model_03/model_13. The old primitive `Resources/Characters/<Role>.prefab` for these 8 roles were DELETED (same as the Flyer/Bomber pass) — `Resources.Load` by role name is ambiguous with both present. Heights are baked in the FBX (1.65-3.5 m) because the FBX path of `CharacterVisual.Create` ignores the height parameter; collider heights in `Enemy.Configure` already matched. `EnemyIdleMotion` now applies to ANY visual without an Animator and without the primitive fallback (`CharacterVisual.UsesFallback`) — not just kind>=11. Verify facing in playtest (same source pack as the 4 known-good models, yaw 0).
- **Village dressing** (`Resources/Props/Village/`, RPGPP_LT pack — the LowPoly Environment Pack was skipped: per-color materials render flat under a shared material): 28 models + `Resources/Props/Textures/rpgpp_lt_tex_a.tga` atlas; `RealmProps.VillageMat(realm)` tints the atlas per realm; `Folder()` routes `rpgpp_lt_` prefix to Village; `PlaceVillage` parks ≤1 structure (shed/well/wagon, 60% on wide islands) lane-safe plus a few crates/barrels/benches and small clutter; sheds/wagons added to `IsBuilding` (mesh collision); RPGPP trees/bushes/flowers mixed into the realm sets. `Place()` now GROUNDS every prop on its real renderer bounds — RPGPP rocks/logs/wagon ship meshes below pivot (negative census `pivotMinY`) and would sink without it.
- **Low-HP vignette + boss title card**: `RealmPost.shader` gained `_Damage` (red edge wash, radius-smoothstepped); `RealmPostFx.OnRenderImage` drives it below 30% HP with an unscaled-time pulse (keeps breathing through HitStop; still gated by `Save.postFx`). Boss guardian levels show a 4.4 s IMGUI title card (`bossIntroUntil`, `titleC` name + "GUARDIAN OF THE <realm>" subtitle, accent rules) fading in/out.
- **Menu music**: Menu/Atlas play `verdant_theme`, Sanctuary plays `frozen_exploration_theme` via `SetMusic(key)` (same-clip no-op); LEVELS keep `boss_battle_theme` everywhere (user request — do not revert). Desert theme remains unused.
- Verified: headless `AsterPhase1.Prepare` exit 0 (28 states), `LostRealms.PropCensus.Run` + `LostRealms.GateProbe.Diagnose` (level-1 build smoke) exit 0, all 6 verify-*.sh pass. New bake took 4 headless runs to get right — the report `ASTER_PHASE1_REPORT.txt` lists every state length.

## Round 6 — progression & challenge pass (Sep 2026)

- **Gem economy**: enemies now drop loot gems on death (`Enemy.ApplyDamage`): boss = 6, Elite mini-boss (kind 14) = 4, affixed elites = 2, regular enemies ≈28% chance of 1. `RealmPickup.Drop(position,count)` (in `RealmWorld.cs`) scatters faceted gem pickups that magnet to the hero exactly like the route gems. Gems count toward the per-level `Gems` → `Save.gems` on `Finish()`, funding the gem-only Sanctuary tracks.
- **Sanctuary skill tree** — 3 new gem tracks (each 3 ranks; rows keep within the 1280×720 IMGUI layout; the gold tracks VITALITY/ARSENAL and ELEMENTAL POWER are untouched): **AETHER** (+25% energy regen/rank, cost 4+2r gems — `Hero.FixedUpdate` regen `dt*(12f+4f*rank)`), **MOXIE** (+1 bonus air dash/rank, 5+2r — `airDashes=1+min(3,moxieRank)` when grounded), **TEMPO** (counter damage +15%/rank and parry energy refund +5/rank, 5+2r), **SECOND WIND** (N free revives per level, 7+3r — revive at `Max(3, 45% MaxHealth)` + 60 energy + 2.2s immunity, tracked by `Hero.windUsed`, reset each level since Hero is recreated). Do not touch the `.2f` parry window or `.55f` cooldown.
- **Combo counter**: `RealmGame.ComboHit()` (called from `Enemy.Hit` on every landed hit) with a 1.1s decay; HUD shows `COMBO xN` at ≥3; reset in `LoadLevel`. Field `Combo` + method `ComboHit()` (can't share the name `Combo` — C# duplicate member).
- **Save versioning**: save key bumped to `LostRealms3D.v2` with a `version=2` field; `Awake` migrates any existing `v1` payload automatically, and new ranks are clamped 0..3. `Persist()` always writes v2.
- **Elite mini-boss**: kind 14 HP scaling raised 2.0× → **2.7×** (`(kind==14?2.7f:1f)` in `Enemy.Configure`), so Elites read as per-realm mini-bosses and pay 4 gems.
- Round-6 RuntimeProbe checks (extended in `Assets/Scripts/RuntimeProbe.cs`, run with `-realmTest` in Play mode): gem pickups spawn on enemy death, combo increments+decays, second wind gives windRank revives then Defeat, aether regen exceeds base 12/s, save persists to v2 key.
- Verified: `compile2.ps1` exit 0, all 6 verify-*.sh exit 0, Android APK rebuilt (`Builds/Android/LostRealms3D.apk`).

## Round 6b — boss mechanics, minimap, boss phase HUD (Sep 2026)

- **Boss mechanics** (`Enemy.cs`): `public int BossPhase` mirrors the private phase; from phase 2 every realm guardian summons its realm's herald minions every ~15s (`SummonHeralds()`, capped at 4 alive within 16 m): realm 0 → BriarGoblin (16), realm 1 → EmberDemon (17), realm 2 → Frost (kind 3). Spawned with `Implosion` + `Portal` VFX and a per-realm `Tell`. In phase 3 the fan-bolt attack becomes a full **12-bolt ring** (`THORN WHEEL`/`CINDER NOVA`/`TEMPEST BURST`). All spawning reuses the existing `Configure(addKind,false,center,area)` minion pattern; `Clamp` keeps adds on the arena.
- **Minimap** (`RealmGame.MiniMap()`, drawn in the Playing HUD at top-right, never overlapping the mobile action row or compass): gold route-dot trail, white GATE marker at the last route node +5 z, red dots for bosses/enemies within 60 m, green square for Aster plus a forward heading tick. Uses `World.Route` bounds with padding. If you relocate mobile buttons, keep `Rect(1032,78,228,150)` free.
- **Boss bar** now shows `PHASE n/3` next to the boss name (uses `BossPhase`).
- RuntimeProbe now also (a) drives the realm-10 boss below 67%/33% and asserts phase 2/3, (b) waits 13 s and asserts a herald (kind 3) exists near the guardian, then clears adds. Requires Play-mode `-realmTest`.
- Verified: `compile2.ps1` exit 0, verify-*.sh 6/6, APK rebuilt.