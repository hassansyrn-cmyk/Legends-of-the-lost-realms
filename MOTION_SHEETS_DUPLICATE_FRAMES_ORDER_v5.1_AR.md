# Legends of the Lost Realms — أمر إصلاح: فريمات مكررة + طفو فوق الأرض (v5.1)

هذا أمر عمل مباشر لإصلاح Build 5.0.0. لا تكتفِ بالتفسير أو الاعتذار — نفّذ التعديلات المطلوبة وسلّم ملفات جديدة تحقق شروط القبول أدناه.

## التشخيص (مثبت بالفحص الفعلي للبكسلات، وليس تخمينًا)

فحصنا كل ملف `*_motion_sheet.png` في `app/src/main/res/drawable-nodpi/` بمقارنة hash لكل خلية في الشبكة. النتيجة: **كل الأعمدة في نفس الصف هي نفس الصورة، منسوخة حرفيًا بكسل بكسل**، وليست فريمات حركة مختلفة. الكود الذي يعرضها (`GameView.java`) يبدّل الفريم بشكل صحيح كل إطار عبر `frameIndex()`، لكن بما أن كل الفريمات المتاحة متطابقة، لا تظهر أي حركة داخل اللعبة — هذا هو سبب "الصور الثابتة" بالضبط.

| الملف | الشبكة الحالية | حجم الخلية | ما فُحص |
|---|---|---|---|
| `aster_motion_sheet.png` | 8 أعمدة × 4 صفوف | 256×256 | كل الـ32 خلية بنفس وضعية الركض تمامًا، فرق طفيف باللون فقط، لا فرق بالوضعية. |
| `enemies_motion_sheet.png` | 6 أعمدة × 8 صفوف | 192×192 | الأعداء الثمانية كلهم بنفس النمط: أعمدة متعددة بنفس الـhash حرفيًا في كل صف. |
| `bosses_motion_sheet.png` | 6 أعمدة × 3 صفوف | 384×384 | نفس المشكلة، لكن هذا الملف غير مستخدم فعليًا في الرسم الحالي (الزعماء يُرسمون عبر rig هيكلي منفصل بالكود) — أصلحه إن كنت ستستخدمه مستقبلًا، وإلا فليس أولوية. |

بالإضافة، فحصنا حشو الشفافية (alpha padding) داخل خلايا `world_motion_sheet.png` (768×1024، 4 أعمدة × 4 صفوف، خلية 192×256):

| الصف | العنصر | حشو فارغ أسفل الرسم داخل الخلية |
|---|---|---|
| 0 | فخ الجذور (Verdant) | 48px من أصل 256px |
| 1 | مدفع النار (Dunes) | 48px من أصل 256px |
| 2 | أشواك الجليد (Frozen) | 44px من أصل 256px |
| 3 | نقطة الحفظ (Checkpoint) | 15px من أصل 256px |

هذا الحشو هو سبب ظهور الأفخاخ ونقطة الحفظ "طايفة" فوق سطح المنصة في اللعب الفعلي، رغم أن كود التموضع يثبّت أسفل مستطيل الرسم على سطح الأرض. **تم إصلاح هذا الجزء مؤقتًا من طرفنا بتعويض بالكود (`groundPadShift` في `GameView.java`)**، لكنه حل مؤقت مبني على أرقام الحشو الحالية فقط — إذا سلّمت شيتات جديدة بحشو مختلف، يجب إخبارنا لتحديث الأرقام (أو الأفضل: سلّم حشوًا شبه معدوم من الأساس، انظر شروط القبول).

## الأمر: المطلوب تنفيذه بالضبط

أعد توليد الملفات التالية فقط (لا تلمس بقية الأصول):
`aster_motion_sheet.png`, `enemies_motion_sheet.png`, `world_motion_sheet.png`

لكل ملف:

1. **حافظ على نفس أبعاد الشبكة وحجم الخلية المذكورة أعلاه بالضبط** (نفس عدد الأعمدة والصفوف)، لأن الكود يقصّ كل خلية بإحداثيات ثابتة مبنية على هذه المقاسات.
2. **كل عمود داخل نفس الصف يجب أن يكون وضعية جسم/طرف مختلفة فعليًا** — ركضة بمراحل قدم مختلفة، رفة جناح بزاوية مختلفة، هجوم بمرحلة مختلفة... إلخ. ليس فرق لون أو إضاءة فقط.
3. **قبل التسليم، تحقق ذاتيًا**: قارن hash أو فرق بكسلات كل عمودين متجاورين في كل صف. إن كانا متطابقين أو متشابهين لدرجة لا تُرى بالعين، أعد توليد ذلك العمود. لا تسلّم ملفًا فيه أي عمودين متطابقين حرفيًا.
4. **قصّ الشفافية بإحكام**: اترك هامشًا شفافًا صغيرًا وموحدًا حول العنصر (8–12px تقريبًا) من كل الجهات (فوق/تحت/يمين/يسار)، وخصوصًا لا فرق كبير بين حشو الأعلى وحشو الأسفل كما حصل في `world_motion_sheet.png` الحالي (وصل الفرق إلى 44–48px تحت مقابل صفر تقريبًا فوق). العنصر يجب أن يلامس تقريبًا حافة الخلية السفلية لأن الكود يثبّت الخلية على سطح الأرض مباشرة.
5. حافظ على نفس الأسلوب الفني (الألوان، الإضاءة، الزاوية) للتناسق مع بقية الأصول الحالية.

## نص جاهز للنسخ المباشر لكل عنصر

```text
Regenerate this sprite sheet as a true multi-frame animation, not a repeated single pose.
Grid: [COLS] columns × [ROWS] rows, each cell [CELL_W]×[CELL_H]px, transparent background (real alpha, no white/checkerboard fringe).
Every column within the same row must show a genuinely different animation frame — different limb/wing/leg position — not a color or lighting variation of the same pose. Verify pixel-by-pixel that no two adjacent frames are identical before returning the file.
Crop each frame tightly: a small, uniform transparent margin (about 8-12px) on all four sides, not a large empty gap on one side only — the bottom of the visible subject should sit close to the bottom edge of its cell.
Match the existing painterly fantasy art style, palette, and camera angle already used in this project.
```

استبدل `[COLS]`/`[ROWS]`/`[CELL_W]`/`[CELL_H]` بالقيم من الجدول أعلاه لكل ملف على حدة.

## شروط القبول قبل ما نعتبرها منجزة

- [ ] فحص hash يدوي أو آلي: صفر أعمدة متطابقة داخل نفس الصف في الملفات الثلاثة.
- [ ] الحشو الشفاف أسفل كل عنصر تفاعلي (فخ/checkpoint) لا يتجاوز ~10-15px، وليس 44-48px كالحالي.
- [ ] نفس أبعاد الشبكة الحالية بالضبط (لا تغيّر عدد الأعمدة/الصفوف بدون تنسيق، لأن الكود سيحتاج تعديلًا مطابقًا).
- [ ] بعد الدمج، إخبارنا برقم الحشو الجديد (إن وُجد) حتى نزيل أو نعدّل `groundPadShift`/`WORLD_ROW_BOTTOM_PAD_PX` في `GameView.java`.
